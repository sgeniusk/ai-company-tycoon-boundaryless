# Alpha v0.3 덱빌딩/로그라이트 QA

작성일: 2026-05-17

## 검증 명령

- `npm test -- src/game/deckbuilding.test.ts`
- `npm run validate:data`
- `npm run build`
- Headless Chrome screenshot: `http://127.0.0.1:5178/?scenario=deck`

## 결과

- 단위 테스트: 통과, 16 tests.
- 데이터 검증: 통과.
- 프로덕션 빌드: 통과.
- 브라우저 렌더링: 통과. 덱 화면에서 `현재 빌드` 패널과 아키타입 점수 표시를 확인했다.

## 발견 이슈

- P0: 없음.
- P1: 없음.
- P2: 시작 덱 선택 버튼이 덱 화면 하단에 있어 1366x768 기준 첫 화면에서는 일부가 아래쪽에 걸릴 수 있다.
- P2: 아키타입 추천 태그가 내부 태그명으로 표시된다. 다음 UI 다듬기 때 한국어 라벨로 바꾸는 것이 좋다.

## 판정

0.3 덱빌딩/로그라이트 기능은 다음 개발로 넘어가도 된다.
