# Alpha v0.14.5 QA 보고서

작성일: 2026-05-16

## 범위

이번 QA는 연간 심사 후 3택1 운영 지시, 저장 복구, 데이터 검증, 24개월 장기 시뮬레이션, `?scenario=review` 화면 상태를 확인한다.

## 자동 검증

| 항목 | 결과 |
|---|---|
| 연간 지시 선택 풀 6개 이상 존재 | 통과 |
| 12개월차 심사 후 선택 후보 3개 생성 | 통과 |
| 후보 선택 시 활성 지시가 선택 후보로 교체 | 통과 |
| 선택 후 `pendingAnnualDirectiveChoices` 제거 | 통과 |
| 레거시 저장 데이터에서 선택 대기 상태 누락 시 안전 복구 | 통과 |
| 24개월 장기 시뮬레이션에서 최소 1회 지시 선택 | 통과 |
| QA review scenario가 3개 후보 노출 | 통과 |
| 데이터 검증이 선택 풀 스키마 검사 | 통과 |

## 실행 명령

- `npm test -- src/game/annual-review.test.ts src/game/run-simulator.test.ts src/game/qa-scenarios.test.ts`: 통과, 33 tests.
- `node scripts/harness/validate-data.mjs`: 통과.
- `npm run harness:gate`: 통과, 145 tests, 데이터 검증 통과, 프로덕션 빌드 통과.
- Headless Chrome screenshot QA: 통과.

## 브라우저 QA 체크리스트

확인 URL:

- `http://127.0.0.1:5173/?scenario=review`
- `http://127.0.0.1:5178/?scenario=review` 최종 확인

| 항목 | 결과 |
|---|---|
| 회사 메뉴에서 연간 심사 카드 표시 | 통과 |
| `다음 해 운영 지시 3택1` 섹션 표시 | 통과 |
| 후보가 오른쪽 패널 폭에 맞는 리스트형 카드로 정리 | 통과 |
| 후보별 월간 효과, 추천 메뉴, 선택 버튼 표시 | 통과 |
| 1366x768에서 주요 텍스트 겹침 없음 | 통과 |

## 이슈

- P0/P1 없음.
- P2: 선택 버튼 이후 즉각적인 시각 피드백은 아직 약하다. 향후 작은 애니메이션/사운드 훅이 필요하다.
- P2: 선택 후보는 현재 결정론적 우선순위다. 덱, 성장 경로, 경쟁사 상태를 반영하는 동적 추천은 다음 단계로 남긴다.

## 결론

자동 QA와 브라우저 스크린샷 기준으로 v0.14.5의 핵심 상태 전환과 화면 표시를 확인했다. P0/P1 없이 다음 단계로 진행 가능하다.
