# Alpha v0.14.8 QA 보고서

작성일: 2026-05-16

## 범위

이번 QA는 `?scenario=reward-bias`가 덱 메뉴, 활성 연간 지시, 보상 편향 카드 후보를 한 번에 재현하는지 확인한다.

## 자동 검증

| 항목 | 결과 |
|---|---|
| `reward-bias` 시나리오 ID 등록 | 통과 |
| URL 파싱 지원 | 통과 |
| 덱 메뉴로 진입 | 통과 |
| 활성 지시가 `신뢰 복리 프로그램` | 통과 |
| 보상 후보에 `상호운용 방어막`, `세이프티 리뷰` 포함 | 통과 |

## 실행 명령

- `npm test -- src/game/qa-scenarios.test.ts`: 통과, 20 tests.
- `npm run harness:gate`: 통과, 149 tests, 데이터 검증 통과, 프로덕션 빌드 통과.
- Headless Chrome screenshot QA: 통과.

## 브라우저 QA 체크리스트

확인 URL:

- `http://127.0.0.1:5178/?scenario=reward-bias`

| 항목 | 결과 |
|---|---|
| 덱 메뉴로 직접 진입 | 통과 |
| QA pill에 `연간 지시 보상 편향 QA` 표시 | 통과 |
| `신뢰 복리 프로그램` 보상 편향 안내 표시 | 통과 |
| 보상 선택 대기 상태 표시 | 통과 |
| 보상 카드 영역과 버튼이 오른쪽 패널 안에 표시 | 통과 |

## 이슈

- P0/P1 없음.
- P2: 아직 보상 카드별 “지시 태그 일치” 배지는 없다. 현재는 상단 안내 스트립으로만 설명한다.

## 결론

v0.14.8은 자동 테스트와 브라우저 스크린샷 기준으로 보상 편향 화면 진입점을 확보했다. P0/P1 없이 다음 단계로 진행 가능하다.
